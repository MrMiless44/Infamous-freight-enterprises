# Prevent npm from generating npm-specific lockfiles; pnpm-lock.yaml is used instead
# Note: package-lock=false also affects pnpm lockfile generation, so we remove it
# and rely on pnpm's default behavior to use pnpm-lock.yaml
fund=false
