# pnpm-lock.yaml is the single source of truth for dependencies
# Note: package-lock=false and shrinkwrap=false are npm-specific and should not be set
# as they interfere with pnpm's lockfile handling
fund=false
