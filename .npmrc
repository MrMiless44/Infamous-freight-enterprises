# Prevent npm from generating lockfiles; pnpm-lock.yaml is the single source of truth
# These npm configs prevent npm from generating package-lock.json or npm-shrinkwrap.json; pnpm-lock.yaml is the source of truth
fund=false
