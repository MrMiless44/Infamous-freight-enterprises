{
  "name": "@infamous-freight/ai-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Worker service for processing AI BullMQ jobs",
  "main": "dist/worker.js",
  "types": "dist/worker.d.ts",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/worker.ts",
    "build": "tsc",
    "start": "node dist/worker.js",
    "lint": "echo 'Linting handled by root config'",
    "test": "echo 'No tests yet' && exit 0"
  },
  "dependencies": {
    "bullmq": "^4.7.0",
    "ioredis": "^5.3.2",
    "@infamous-freight/shared": "file:../shared"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.0"
  }
}
