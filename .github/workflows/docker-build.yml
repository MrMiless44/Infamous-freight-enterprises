name: Build Docker Images

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  validate-html:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Install tidy
        run: sudo apt-get update && sudo apt-get install -y tidy
      
      - name: Validate HTML files
        run: |
          echo "Validating HTML files..."
          failed=0
          while IFS= read -r -d '' html_file; do
            echo "Validating $html_file"
            if ! tidy -e -q "$html_file"; then
              failed=1
            fi
          done < <(find . -name "*.html" -not -path "./node_modules/*" -not -path "./.git/*" -not -path "./.next/*" -type f -print0)
          if [ $failed -eq 1 ]; then
            echo "HTML validation failed!"
            exit 1
          fi
          echo "All HTML files validated successfully!"

  build:
    runs-on: ubuntu-latest
    needs: validate-html
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build API image
        uses: docker/build-push-action@v5
        with:
          context: ./api
          push: false
          tags: ghcr.io/${{ github.repository_owner }}/infamous-freight-api:test

      - name: Build WEB image
        uses: docker/build-push-action@v5
        with:
          context: ./web
          push: false
          tags: ghcr.io/${{ github.repository_owner }}/infamous-freight-web:test
